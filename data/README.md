# Data

The original coursework file `IPI_Esp.xlsx` is **not redistributed** in this public portfolio repository.

To reproduce the notebook, place the source file at:

```text
data/IPI_Esp.xlsx
```

Expected columns:

- `Date` — monthly period encoded as `YYYYMmm`, e.g. `1975M01`
- `IPI Nacional` — Spanish Industrial Production Index value
